{
  "version": 2,
  "builds": [
    { "src": "**/*.js", "use": "@now/node" },
    { "src": "static/**/*.json", "use": "@now/node" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "serverless.js" }
  ],
  "env": {
    "MONGODB_URI": "@mongodb-uri"
  }
}